ip = "127.0.0.1";
log = {
    path = "/opt/hvs/var/log/hvs.log";
    level = 10;
};

manager_addr = ("127.0.0.1", );

// only ioproxy node need these settings
rpc = {
  port = 9092;
  workers = 6;
  timeout = 3000;
  retry = 3;
};

ioproxy = {
    //use linux command uuid to generate your own uuid
    uuid = "a8a8eb78-8536-4bb5-a22a-c83483eff378";
    manager_addr = "127.0.0.1";
    scher = 6;
    data_path = "/opt/hvs/var/data";
    data_port = 9095;
    data_buffer = 1024000;
    data_conn = 50;
};

manager = {
    //use linux command uuid to generate your own uuid
    uuid = "6305fe8e-7fbd-11e9-a2ea-27b96962514c";
};

// only manager node need these settings
couchbase = {
    address = "192.168.10.235";
    user = "dev";
    password = "buaaica";
    bucket = "test";
};

rest = {
    port = 9090;
    thread_num = 6;
};

// only client node need these settings
fuse = {
    mountpoint = "/mnt/hvs";
    foreground = false;
    debug = false;
    multithread = true;
    auto_unmount = true;
    use_udt = true;
};

client = {
    data_port_begin = 9096;
    data_port_end = 9150;
    data_buffer = 1024000;
};